import styles from '@/Style/ComponentStyle-EXPOTEC.module.css'

const ExpositoresContent = () => {
	return (
		<>
			<header className={styles.SubHeader}>
				<strong className={styles.HeaderTitle}>
					Expositores / EXPOTEC / 2024
				</strong>
			</header>
			<div className={styles.ContainerExhibitors}>
				<div className={styles.gallery}>
				</div>
			</div>
		</>
	);
};

export default ExpositoresContent
